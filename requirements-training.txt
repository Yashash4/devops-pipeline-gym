# Phase 6 training deps — install on GPU host (Kaggle T4 / H100).
# NOT installed on the HF Space (CPU-only; these need CUDA).
#
# Usage:
#   pip install -r requirements.txt            # env deps (incl. CPU-only)
#   pip install -r requirements-training.txt   # GPU training + eval deps
# OR:
#   pip install -e '.[training]'               # same set via pyproject extras
#
# Versions validated on Kaggle T4 dry-run 2026-04-24:
#   unsloth 2026.4.8, trl 0.29.x, torch 2.10.0+cu128, transformers 4.56.2.
# Older unsloth 2025.9.x is known-bad on the current Kaggle image — pin
# the floor to 2026.4 to avoid accidentally pulling that branch.
#
# torch + transformers are NOT pinned here — Kaggle / HF compute provides
# them pre-installed at known-good versions that align with their CUDA
# driver. A local pin would fight those wheels.

unsloth>=2026.4.0
trl>=0.29.0
peft>=0.10
bitsandbytes>=0.43
datasets>=2.20
matplotlib>=3.8
accelerate>=0.30
