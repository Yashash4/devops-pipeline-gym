# Phase 6 — Round 2 training pipeline. See grpo_train.py, eval_baseline.py,
# and generate_comparison_chart.py. This directory is EXCLUDED from the HF
# Space Docker image (see .dockerignore) so training dependencies (Unsloth,
# TRL, bitsandbytes, CUDA libs) don't bloat the CPU-only Space.
